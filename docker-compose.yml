services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: sessionbook
      POSTGRES_USER: sessionbook_user
      POSTGRES_PASSWORD: localdevpassword
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: